/**: 
  ros__parameters:
    acceptable_max_delay: 1.0
    gnss_mahalanobis_distance_threshold: 40.0 # If the distance to GNSS observation exceeds this, the correction is skipped.
    use_ublox_msg: true # Raw sensor data should not be used when running as Autoware.
    ignore_less_than_float: true
    for_fixed/max_weight: 5.0